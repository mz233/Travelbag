<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mazhuang.travelbag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainFrame">

        </activity>
        <activity android:name=".FetchActivity" />
        <activity android:name=".Wait_Fetch_Activity" />
        <activity android:name=".CreateOrder" />
        <activity android:name=".Payment_Activity" />
        <activity android:name=".PayWay" />
        <activity android:name=".order_main" />
        <activity android:name=".Self" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>